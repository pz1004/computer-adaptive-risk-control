# CARC Redesign Status

## 2026-06-16

- Step 1 complete: Loaded the required communication, planning, coding, and results-analysis skills; confirmed `status.md` exists and is empty; inspected the existing planning/evidence files without overwriting prior work.
- Current focus: Inventory the selector and experiment architecture, then choose the smallest valid redesign that can be proved, implemented, and tested before expanding to broad real-cache evidence.
- Step 2 complete: Inspected `carc/selector.py`, `carc/certify.py`, `carc/pvalues.py`, `carc/chain.py`, `tests/test_carc.py`, and the synthetic/cache experiment scripts. Current valid selectors are one-chain fixed sequence, Holm, Bonferroni, plus naive validation inspection.
- Design direction: Implement multi-chain CARC as the first redesign. It searches several pre-specified compute-ordered chains, spends `delta / M` per chain, controls FWER by a union bound over chains, and should improve power over full-grid Bonferroni/Holm when `M << K` while covering more policies than the existing scalar chain.
- Step 3 complete: Implemented `carc.certify.multi_chain` and routed `selector.select_risk(..., method="multichain", chains=...)` through it. Added tests for alpha-weight validation, union-of-fixed-sequence behavior, and a case where multi-chain selects a cheaper certified policy than the default single chain.
- Verification: `python -m py_compile carc/certify.py carc/selector.py tests/test_carc.py` passed; `python -m tests.test_carc` passed all tests, including Monte Carlo validity checks.
- Current focus: Add a fast synthetic experiment that creates a richer multi-chain candidate family and compares multi-chain against chain, Holm, Bonferroni, and naive under true oracle risks.
- Step 4 complete: Added `carc.chain.build_threshold_family`, a generic per-exit threshold-vector builder that returns the existing `loss_matrix`/`cost_matrix` contract. Added an equivalence test showing repeated scalar threshold vectors exactly match `build_chain`.
- Verification: `python -m py_compile carc/chain.py tests/test_carc.py` passed; `python -m tests.test_carc` passed all tests with the new threshold-family helper.
- Step 5 complete: Added `experiments/e8_multichain_selector.py`, which constructs six pre-specified per-exit threshold chains, compares scalar-chain, multi-chain, Holm, Bonferroni, and naive selectors under oracle synthetic risk, and writes JSON evidence.
- Smoke check: `python -m experiments.e8_multichain_selector --out results/e8_multichain_selector_smoke.json --n 300 --T 20 --oracle-n 20000 --levels-per-chain 12` ran successfully; the smoke artifact is temporary and will be removed before the fuller run.
- Step 6 complete: Ran the fuller seeded E8 synthetic redesign artifact: `python -m experiments.e8_multichain_selector --out results/e8_multichain_selector.json --n 1000 --T 1000 --oracle-n 250000 --levels-per-chain 30`.
- E8 result summary: multi-chain used 6 chains and 180 candidates. Across all six `(alpha, delta)` cells, multi-chain true-violation rate was `0.000`--`0.001`, below `delta`; naive violated at `0.363`--`0.382`. Multi-chain had lower mean true cost than Holm and Bonferroni in every cell, higher feasibility than the scalar chain at the strictest target, and lower mean cost than the scalar chain in 5 of 6 cells.
- Verification: `results/e8_multichain_selector.json` passed `python -m json.tool`; `carc.__init__` now exports `build_threshold_family`; `python -m py_compile carc/__init__.py carc/certify.py carc/chain.py carc/selector.py experiments/e8_multichain_selector.py tests/test_carc.py` passed.
- Current focus: Extend real-cache evaluation to support richer multi-chain candidate families before changing manuscript claims.
- Step 7 complete: Added `experiments/multichain_family.py` and extended `experiments.real_cache_eval` with `--family multichain`, using existing cache arrays (`scores`, per-exit `loss`, and `exit_cost`) to build richer per-exit threshold families without retraining.
- Smoke check: A tiny digits multi-chain run completed successfully, then the smoke JSON was removed.
- Step 8 complete: Ran the full digits real-cache multi-chain artifact: `python -m experiments.real_cache_eval --cache cache/digits_tabular_cascade.npz --out results/digits_tabular_multichain_eval.json --family multichain --levels-per-chain 30 --threshold-low 0.0 --threshold-high 1.0 --alphas 0.05,0.10,0.15 --deltas 0.05,0.10 --calib-sizes 100,300 --T 1000 --seed 20260616 --pvalue hb --violation-denominator all`.
- Digits multi-chain result summary: At `alpha=0.10`, multi-chain held-out violation was `0.002`--`0.013`, below both tested deltas, and mean test cost was about `2.78`--`2.83`; scalar-chain cost was about `6.05`--`6.08`, while naive violated at `0.442`--`0.542`. At `alpha=0.15`, multi-chain cost dropped to `1.58`--`2.65` with violation `0.000`--`0.016`.
- Verification: `results/digits_tabular_multichain_eval.json` passed `python -m json.tool`; compile checks for `experiments/multichain_family.py` and `experiments/real_cache_eval.py` passed.
- Step 9 complete: Ran the existing CIFAR-100 branchy cache through the new multi-chain evaluator: `python -m experiments.real_cache_eval --cache cache/cifar_branchy_resnet56.npz --out results/cifar_branchy_multichain_eval.json --family multichain --levels-per-chain 30 --threshold-low 0.0 --threshold-high 1.0 --alphas 0.30,0.35,0.40 --deltas 0.05,0.10 --calib-sizes 500,2000 --T 1000 --seed 20260616 --pvalue hb --violation-denominator all`.
- CIFAR multi-chain result summary: Multi-chain did not make the old `alpha=0.30` frontier meaningfully feasible. For feasible neural-cache cells, multi-chain reduced cost relative to Holm/Bonferroni across `alpha=0.35/0.40`; at `alpha=0.40, n_cal=2000`, it was feasible in all trials, stayed below delta (`0.007` at `delta=0.05`, `0.011` at `delta=0.10`), and had lower mean cost (`86.59M`/`85.95M` MACs) than scalar-chain (`87.22M`/`86.71M`), Holm, and Bonferroni.
- Verification: `results/cifar_branchy_multichain_eval.json` passed `python -m json.tool`; `git diff --check` passed after the code and experiment additions.
- Step 10 complete: Built two additional tabular caches with the existing adapter: `cache/wine_tabular_cascade.npz` and `cache/breast_cancer_tabular_cascade.npz`.
- Step 11 complete: Ran multi-chain real-cache evaluations for both extra tabular datasets, writing `results/wine_tabular_multichain_eval.json` and `results/breast_cancer_tabular_multichain_eval.json`.
- Extra tabular result summary: Breast cancer is useful supporting evidence: at `alpha=0.15, n_cal=120`, multi-chain had violation `0.000`, feasibility `0.986`--`0.995`, and mean cost `1.1`--`1.3`, below scalar-chain and Holm/Bonferroni. Wine is mostly a saturated boundary case: all exits are near-perfect, and most strict cells are infeasible for certified selectors.
- Verification: `results/breast_cancer_tabular_multichain_eval.json` and `results/wine_tabular_multichain_eval.json` passed `python -m json.tool`; `python -m py_compile` passed for all touched Python files; `python -m tests.test_carc` passed after the selector, threshold-family, evaluator, and extra-cache work.
- Current focus: Add the multi-chain theorem/proof and evidence summary to `main.tex` without overstating the still-limited neural evidence.
- Step 12 complete: Updated `main.tex` with the multi-chain selector contribution, Proposition 1 and proof, E8 synthetic evidence, digits/breast-cancer/wine/CIFAR multi-chain evidence summaries, limitations for pre-specified chain design, conclusion updates, and the new JSON artifact list.
- Manuscript verification: Text-marker checks found no stale hard-coded `Section~10.2`/`Section~10.3` references after the new subsection insertion. `git diff --check`, Python compile checks, all new JSON validations, and `python -m tests.test_carc` passed.
- Remaining build gate: `tectonic --keep-logs --keep-intermediates main.tex` still fails immediately because `ieeeaccess.cls` is absent in the local environment. The generated `main.log` from that failed check was removed.
- Current focus: The redesign is implemented and manuscript-integrated, but the larger objective is not complete yet. The main missing evidence for the 75--85% IEEE Access target is a stronger newly trained feasible neural early-exit or cascade benchmark, preferably with measured latency or at least a stronger MAC-based vision/text cascade.
- Step 13 start: Resuming the broad-evidence phase. Inspected `adapters/cifar_branchy.py`, current caches/checkpoints, and GPU availability. CUDA is available on `Quadro RTX 8000`. The existing adapter is currently hard-coded to CIFAR-100 in dataset choices, normalization, `num_classes=100`, fake-data class count, and metadata.
- Current focus: Generalize the CIFAR branchy adapter to support CIFAR-10 while preserving existing CIFAR-100 behavior, then train/evaluate a feasible CIFAR-10 branchy cache as the stronger neural benchmark.
- Step 14 complete: Generalized `adapters/cifar_branchy.py` to support CIFAR-10 while preserving CIFAR-100 and fake-data behavior. `dataset_spec` now reports CIFAR-10 with 10 classes and CIFAR-10 normalization, CIFAR-100 with 100 classes and the original normalization, and FakeData with the original 100-class smoke-test behavior.
- Verification: `python -m py_compile adapters/cifar_branchy.py` passed. A fake-data adapter smoke run wrote `cache/smoke_cifar_branchy_multidataset.npz` with `n=32`, `4` exits, and `8` thresholds; reported per-exit accuracies were `0.0000 0.0000 0.0000 0.0312`, and the expected four analytical MAC costs were emitted. The two temporary smoke artifacts were removed and confirmed absent.
- Current focus: Train or resume a CIFAR-10 branchy cache, then evaluate the multi-chain selector on a feasible neural benchmark with saved JSON artifacts before changing manuscript claims.
- Step 15 progress: Started the CIFAR-10 branchy neural-cache build with `python -u -m adapters.cifar_branchy --dataset cifar10 --download --epochs 30 --checkpoint checkpoints/cifar10_branchy_resnet56.pt --out cache/cifar10_branchy_resnet56.npz --batch-size 256 --num-workers 4 --threshold-count 101 --seed 20260616 --save-every 10`. CIFAR-10 downloaded successfully after a visible `170M` archive transfer. Epoch 10 completed with loss `0.8068` and final-head training accuracy `0.8018`, then wrote `checkpoints/cifar10_branchy_resnet56.pt` at epoch 10. Training continued toward epoch 30.
- Step 15 complete: CIFAR-10 branchy training finished at epoch 30 with loss `0.4695` and final-head training accuracy `0.9361`, saved `checkpoints/cifar10_branchy_resnet56.pt` at epoch 30, and wrote `cache/cifar10_branchy_resnet56.npz`.
- CIFAR-10 cache summary: `n=10000`, `4` exits, `101` thresholds, per-exit test accuracies `0.2551 0.6928 0.8750 0.8825`, and analytical exit MACs `442528 42909856 84328768 125747840`.
- Step 16 complete: Fixed CIFAR cache metadata bookkeeping in `adapters/cifar_branchy.py` so regenerated caches record `loaded_checkpoint_epoch`, `checkpoint_epoch`, and `completed_epoch` consistently. Rebuilt `cache/cifar10_branchy_resnet56.npz` from the saved epoch-30 checkpoint; the command loaded epoch 30, skipped training, and rewrote the cache.
- Verification: `python -m py_compile adapters/cifar_branchy.py` passed. The regenerated CIFAR-10 cache now reports `loaded_checkpoint=True`, `loaded_checkpoint_epoch=30`, `checkpoint_epoch=30`, `completed_epoch=30`, `epochs=30`, and unchanged per-exit accuracies `0.2551 0.6928 0.8750 0.8825`.
- CIFAR-10 frontier check: With the default six-chain multi-chain family (`180` candidates), the full-cache risk floor is about `0.1173`. `alpha=0.10` is infeasible, while oracle-feasible candidate counts are `24` at `alpha=0.12`, `65` at `alpha=0.15`, `90` at `alpha=0.20`, `109` at `alpha=0.25`, and `125` at `alpha=0.30`.
- Step 17 complete: Ran the CIFAR-10 branchy neural-cache multi-chain evaluation: `python -u -m experiments.real_cache_eval --cache cache/cifar10_branchy_resnet56.npz --out results/cifar10_branchy_multichain_eval.json --family multichain --levels-per-chain 30 --threshold-low 0.0 --threshold-high 1.0 --alphas 0.12,0.15,0.20,0.25,0.30 --deltas 0.05,0.10 --calib-sizes 500,2000 --T 1000 --seed 20260616 --pvalue hb --violation-denominator all`.
- CIFAR-10 evaluation summary: `results/cifar10_branchy_multichain_eval.json` has `100` rows and passed `python -m json.tool`. For `n_cal=2000`, multi-chain had lower mean test cost than scalar-chain in all `10/10` alpha/delta cells. In the useful feasible range `alpha=0.15`--`0.30`, multi-chain violation rates were `0.001`--`0.017`, feasible rates were `0.956`--`1.000`, and mean costs ranged from `74.87M` MACs at `alpha=0.15, delta=0.05` down to `45.61M` MACs at `alpha=0.30, delta=0.10`. Naive violation rates over all CIFAR-10 cells ranged from `0.382` to `0.547`.
- Step 18 complete: Updated `main.tex` to integrate the CIFAR-10 feasible neural-cache benchmark. Changes include the abstract/Introduction evidence summary, contribution and evidence-scope wording, the experimental-roadmap paragraphs, a new main-text `Table~\ref{tab:cifar10-multichain}` with the eight `n_cal=2000` CIFAR-10 multi-chain cells, the limitations/conclusion framing, data/code artifact list, and the extended experimental detail paragraph for the CIFAR-10 protocol.
- Verification: `python -m tests.test_carc` passed. `python -m py_compile adapters/cifar_branchy.py experiments/real_cache_eval.py experiments/multichain_family.py experiments/e8_multichain_selector.py` passed. All `13` JSON files under `results/` parsed successfully. A table-consistency script checked all eight CIFAR-10 table rows against `results/cifar10_branchy_multichain_eval.json` with zero missing rows. `git diff --check` passed. `tectonic --keep-logs --keep-intermediates main.tex` still fails immediately at `main.tex:2` because `ieeeaccess.cls` is not installed locally; generated TeX intermediates were removed and confirmed absent.
- Step 19 complete: Advanced the IEEE Access package gate from source-only failure to a successful local PDF rebuild. Added the IEEE Access class/assets currently available in the worktree, patched the local class so the Access footer uses `\theyear` instead of a hard-coded `2016`, fixed the running-head spacing in `main.tex`, abbreviated the method-comparison table headers, wrapped the long data/code artifact paragraph, and split the calibration-estimator display to remove source-owned overfull boxes outside the title page.
- PDF verification: `tectonic --keep-logs --keep-intermediates main.tex` exits `0` and writes `main.pdf` (`20` pages, letter size, `279733` bytes). `pdftotext` confirms `VOLUME 14, 2026`, `S. Jang et al. Compute-Adaptive Risk Control`, and the CIFAR-10 `Table 3` marker. Visual page checks were performed on pages 1, 9, 13, and 20 from `/tmp/carc_pdf_pages/`; the footer year, running head, main evidence page, data/code page, and biographies render legibly.
- Package/readiness verification: `python -m tests.test_carc` passed; `python -m py_compile adapters/cifar_branchy.py experiments/real_cache_eval.py experiments/multichain_family.py experiments/e8_multichain_selector.py carc/certify.py carc/selector.py carc/chain.py tests/test_carc.py` passed; all `13` result JSON files under `results/` passed `python -m json.tool`; `git diff --check` passed. `main.log`/`main.blg` show no undefined references, undefined citations, BibTeX errors, fatal TeX errors, or emergency stops. Remaining local-build caveats are Tectonic font-substitution warnings, the recurring IEEE Access class-output `505.12177pt` overfull warnings, and two visually harmless `\maketitle` overfull warnings on the title page.
- Current focus: The redesigned selector, proofs, broad synthetic/real-cache evidence, and local IEEE Access PDF are now in place. The remaining submission caveat is template provenance: the shell download of the official IEEE Access ZIP was blocked earlier by HTTP 403, so the mirrored/patched local class should be replaced or cross-checked against the official IEEE Access LaTeX package before final upload.
- Step 20 complete: Resolved the official-template provenance caveat. Downloaded the current official IEEE Access LaTeX ZIP from `https://ieeeaccess.ieee.org/wp-content/uploads/2026/05/ACCESS_latex_template_20260513-1-1.zip` to `/tmp/ieee_access_official/ACCESS_latex_template_20260513-1-1.zip`; the ZIP SHA-256 is `60c7efc9db8ac9e8bdb31c550ad4e03cb6f258a878ececc0bc690b6203e45a67`, and it expands to `ACCESS_latex_template_20260513`.
- Official template integration: Replaced the mirrored local package with the official `ieeeaccess.cls`, `IEEEtran.cls`, `IEEEtran.bst`, `spotcolor.sty`, `logo.png`, `notaglinelogo.png`, `bullet.png`, Type 1 font files, `.tfm` files, `.fd` files, and font map files. Before the compatibility patch, the local `ieeeaccess.cls`, `logo.png`, `notaglinelogo.png`, and `bullet.png` hashes matched the official ZIP exactly.
- Minimal local class patch: The unmodified official class failed under Tectonic at `ieeeaccess.cls:281` in the `spotcolor` block. The only class diff now wraps that official `spotcolor` block in `\ifpdf`; pdfTeX keeps the official Pantone spot-color path, while non-pdfTeX uses the same CMYK Access blue fallback for local Tectonic/XeTeX builds.
- Manuscript compatibility patch: The official class's older `\textbf` definition failed on one bold phrase containing inline math. Changed only that phrase from `\textbf{$L^1$ error budget}` to `$L^1$ \textbf{error budget}`. Also replaced the earlier local `\Year{2026}` shim with the official class command `\year{2026}`.
- Phase 8 verification: `tectonic --keep-logs --keep-intermediates main.tex` exits `0` against the official template bundle plus the one-block engine guard and writes `main.pdf` (`20` pages, letter size, `297153` bytes). `pdftotext` confirms `Date of current version June 15, 2026`, `VOLUME 14, 2026`, `S. Jang et al. Compute-Adaptive Risk Control`, and the CIFAR-10 `Table 3` marker. Visual page checks on pages 1, 9, 13, and 20 render cleanly.
- Phase 8 code/result verification: `python -m tests.test_carc` passed; Python compile checks for the touched adapter, evaluator, experiment, core, and test files passed; all `13` result JSON files under `results/` parsed with `python -m json.tool`; `git diff --check` passed. Remaining local-build caveats are non-fatal Tectonic/font warnings, recurring IEEE Access class-output `505.12177pt` overfull warnings, and small visually harmless title/remark overfull warnings.
- Current focus: The scientific redesign, proof integration, broad evidence, official-template source package, and local PDF gate are complete. I am not marking the whole thread goal complete yet because `main.tex` still contains user-supplied submission placeholders: manuscript DOI, source repository/archive DOI, and acknowledgments/funding/AI-disclosure text.
- Step 21 complete: Ran the final scientific completion audit for the redesign objective. Code evidence: `carc.certify.multi_chain`, `selector.select_risk(..., method="multichain")`, `carc.chain.build_threshold_family`, `experiments/e8_multichain_selector.py`, `experiments/multichain_family.py`, and `experiments.real_cache_eval --family multichain` are present and covered by `tests/test_carc.py`. Manuscript evidence: Proposition 1 gives multi-chain validity, the abstract/introduction state the redesigned selector and evidence scope, Table 3 reports CIFAR-10 neural-cache multi-chain evidence, and the data/code section lists the row-level JSON artifacts.
- Final evidence audit: Multi-chain synthetic E8 has `30` rows in `results/e8_multichain_selector.json`; the real-cache multi-chain artifacts have `60` rows each for digits, breast cancer, wine, and CIFAR-100, and `100` rows for CIFAR-10. The final code/build checks remain green: `python -m tests.test_carc`, Python compile checks, all `13` result JSON parses, `git diff --check`, and the IEEE Access `tectonic` build all pass.
- Final acceptance-readiness estimate: after the user-supplied upload metadata is filled, the current scientific version is estimated at `75%--82%` acceptance probability for IEEE Access. The main reasons are: finite-sample validity proof, a clearly stronger multi-chain selector, broad synthetic plus real-cache evidence including a feasible CIFAR-10 neural benchmark, honest CIFAR-100 infeasibility reporting, official IEEE Access formatting, and explicit limitations. The remaining risk is not a missing core experiment; it is reviewer preference for larger production-scale benchmarks or measured latency, which is already framed as future work.
- Metadata boundary: `main.tex` still contains values that should come from the authors or submission system rather than be invented here: the manuscript DOI, source repository/archive DOI, and acknowledgments/funding/AI-disclosure text. These are pre-upload metadata tasks, not evidence gaps in the redesign.
